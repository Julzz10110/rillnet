version: "2"

run:
  timeout: 5m

linters:
  default: standard
  exclusions:
    paths:
      - ^examples/
      - ^tests/
      - _test\.go$
      - builtin$

issues:
  max-issues-per-linter: 50
  max-same-issues: 10
